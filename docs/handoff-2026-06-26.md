# Session handoff — 2026-06-26 (resume on another PC)

This is the **planner** session's running state. Everything below is on `master`
and **pushed to origin** (clean tree — only `client/vite.config.run.mjs` is an
untracked throwaway). On the other PC: `git pull` gets it all.

## TL;DR — to resume
1. `git pull` · `pnpm install` · `pnpm build` (shared → server → client).
2. **Rebuild + restart the dev server.** The last live session was running a
   *stale* `dist/` (pre-cache, pre-dictionary-route) — that's why Standard codes
   was still ~8 s and the new routes 404'd. After build: `pnpm dev`.
3. **You need a cht-conf project folder on the new PC.** The app edits an external
   folder; last used `D:\medic\config-nssd\chis` (won't exist there). Clone/obtain
   a config and point the app at it on the first screen.
4. **Populate the Standard-codes dictionaries** — they're currently empty
   (`shared/src/fhir/dictionaries/` has only a README), which is why the picker
   shows "only a few codes":
   ```sh
   pnpm --filter @cht-ui/shared build
   node scripts/build-terminology-pack.mjs --systems=loinc,ciel   # free sources, works now
   # ICD-10 + ICD-11: register free WHO creds at https://icd.who.int/icdapi/
   #   export WHO_ICD11_CLIENT_ID=... WHO_ICD11_CLIENT_SECRET=...
   #   node scripts/build-terminology-pack.mjs
   ```

## What shipped this session (all pushed; gates green: 258 shared tests, typecheck clean)
| Area | Commits | State |
|---|---|---|
| **Perf parsed-form cache** (fixes ~8 s load) | `5315a35` | ✅ code done + tested; needs server restart to take effect |
| **Dictionary population** (broad-vendored: snapshot script + `/api/fhir/dictionary/search` + picker switch) | `0ccf97d` | ✅ pipeline done; **data NOT yet snapshotted** (run the script) |
| **Targeted deploy** (pick which forms to convert/upload + git-changed detect) | `d1575c0`, `1813fa0`, `e065fec`, `957a98b` | ✅ |
| **FHIR code-chip contrast** (the "can't read the codes" bug, B3a) | `40d33b6`, `c7cb635`, `749bc7b`, `43d6fd0` | ✅ |
| **Survey structural-issues popover → click to jump to row** (H3) | `67a625c` | ✅ |
| **e2e demo + mini-config hierarchy** | `5631212`, `ffa9fc5`, `21fb47d` | ✅ |

## Verified (audit 2026-06-26)
Engineering for all the above is committed and **correct** — routes wired
(`registerDictionaryRoutes` in `index.ts`), cache has a test suite
(`parsedFormCache.test.ts`), 258 tests pass, typecheck clean. The *only* reasons
the live app still showed the old behavior were the two operational gaps in the
TL;DR (stale server + un-run snapshot) — **not** missing work.

## Open decisions / blockers
- **ICD-10 variant:** the dev chose **WHO ICD-10** (`icd-10-who.json`) — the
  international standard MoH-Nepal expects. Confirm with the MoH/clinical owner.
- **WHO API creds** (free) needed before ICD-10/ICD-11 can be snapshotted.
- Online typeahead enrichment is **out of scope** (broad-vendored offline was chosen).

## Source-of-truth plans (in `docs/plans/`)
`perf-parse-cache.md` · `fhir-pack-population.md` (broad-vendored locked) ·
`deploy-targeted-forms.md` · `fhir-v1-triad-punchlist.md` ·
`shipped-batch-triad-punchlist.md` · `survey-groups-and-scaffold.md` ·
`calc-reference-builder.md` · `calculation-builder.md` · `condition-builder.md` ·
`fhir-standard-codes-mvp.md`. Personas mirror: `docs/personas.md`.

## What's next (dev queue)
1. **Run the dictionary snapshot** (with WHO creds) → resolves "only a few codes".
2. Optional: QA + Developer lens over the **parse cache** (it's on the
   round-trip-critical path — a stale cache could feed a wrong serialize) and the
   zero-SNOMED-at-scale filter.
3. Remaining punch-list items: FHIR V1 (blockers/highs beyond B3a),
   shipped-batch (B1/B2…), survey-groups A5 "Group these" wrap (still deferred).
4. Known pre-existing: `helper-builder.spec.ts` fails on mini-config (no
   contact-summary fixture); e2e isn't in CI, so it slipped — worth adding both.

## Working model
Planner session = scope/design/plan docs + persona memory. Developer session =
implements. Coordinate via `docs/` + git. **Note:** persona memory is
user-scoped (outside the repo, under `~/.claude/...`) — it won't travel via
`git pull`; `docs/personas.md` is the committed mirror.
