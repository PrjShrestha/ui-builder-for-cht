// Targets definition (required by cht-conf to compile app_settings).
// Each entry computes one analytic per contact. The UI Builder does not
// edit targets in v0.1 — leave this as `[]` or add entries by hand.
// Reference: https://docs.communityhealthtoolkit.org/apps/reference/targets/
module.exports = [];
