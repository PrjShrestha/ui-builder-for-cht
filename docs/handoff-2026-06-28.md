# Session handoff — 2026-06-28 (4-day summary, resume state)

**Covers 2026-06-24 → 2026-06-28 · HEAD `b4d837b` (master, pushed to `origin/master`).**
Continue from the "What's left / next" section. Companion docs: the live dev queue
is `docs/DEV-HANDOFF.md` ("Do next"); the milestone audit is
`docs/reviews/after_hierarchy_and_contacts.md`.

## TL;DR
Four heavy days. The FHIR Standard-codes track, a server perf cache, targeted
deploy, the lineage-block generator, the whole onboarding + contact-form-generator
track, and form-to-form data passing (Phase 0/1/2a) all shipped. In the last day
the **quick hierarchy creator** (greenlit + spec'd this session) and the **Phase-1
UI wiring gap** were both implemented by the dev. Build is green (typecheck + shared
build); tests 333/336 (3 CRLF-only); lint still RED (~90, deferred). ~40k insertions
across the window — heavily inflated by the LOINC dictionary snapshot (4,498 codes).

## What shipped (by theme)

### FHIR / Standard codes
- **B3a/B1/B2/B3/H3 punch-list fixes** — code-chip contrast (`43d6fd0`,`749bc7b`),
  dictionary-selector a11y native radio (`29466ea`), status non-color glyph cue
  (`32eb331`), orphans visible in Workbench + DecisionsView / MOH gate (`62ac97c`),
  P1 round-up incl. B1 docstring (`97b2452`).
- **Dictionary population** — snapshot pipeline + server-side search + picker switch
  (`0ccf97d`); **LOINC snapshot committed** (4,498 codes, LOINC-2.78, `21efc9f`),
  loaded from disk, zero runtime API. ICD-10/11 + CIEL still pending (see open items).

### Performance
- **Parsed-form cache** (`5315a35`) — warm `/api/fhir-mapping` **8420ms → 4ms** on
  config-nssd (69 forms), keyed by absPath+mtime+size, deep-frozen, `invalidate()`.

### Deploy
- **Targeted form deploy** (`d1575c0`) — form picker (convert/upload app+contact),
  git-changed detection (`GET /api/forms/changed`), command preview, `extraArgs`.

### Survey / lineage
- **One-click "Add lineage"** (`1534c65`) — inserts a contact + ancestor-places
  block; full codegen + 26 tests. Variant A only; Variant B (contact-edit) deferred.
- Toast hidden-row count fix (`51e1620`).

### Onboarding + contacts
- **Onboarding guardrails** (`525c649`) — Forms-tab empty-`contact_types` nudge,
  hierarchy-first cue, deploy-readiness checklist. **New-project picker kept as-is**
  per user (`27e2939`). Guide-don't-gate (nothing disabled).
- **Contact-form generator** (`6d65502`) — offered in the Hierarchy editor; generates
  **minimal-valid** create/edit per type; skip-not-overwrite;
  `POST /api/forms/generate-contact`. Closes the AddTypeForm dangling-form contract.

### Form-to-form data passing
- **Phase 0** — `summaryFlags` wired into PropertiesEditor (`b022787`).
- **Phase 1a+1b** — cross-form refs (contact-input + contact-summary) usable in
  relevant/constraint/choice_filter at the parser layer (`908ddd9`); 2 new additive
  rule kinds, round-trip byte-stable (27 adversarial probes, 0 drift).
- **Phase 2a** — structured `modifyContent` field mappings in tasks.js (`558bf5f`),
  byte-range edit preserved, raw fallback intact.
- **Adversarial-review fixes** — 7 HIGH severity, incl. BUG #7 arg-list inflation and
  string-aware tokenizer fixes (`3126a01`).

### This session (2026-06-28)
- **Quick hierarchy creator** — greenlit, triad-reviewed, spec'd
  (`docs/plans/quick-hierarchy-creator.md`), queued #1, then **implemented by the dev**
  (`b4d837b`). Guided empty→deployable scaffolder for the empty template.
- **Phase-1 UI wiring gap closed** (`be8279f`) — the relevant/constraint/choice_filter
  mounts now pass `inputContactFields`/`contextKeys` (was the one open audit finding).
- **Milestone audit + queue refresh** (`d1ffe82`).

## Decisions locked (planner)
- New-project picker stays **as-is**; onboarding is guardrails only.
- Contact-form generation is **offered, not automatic**; **minimal-valid** (Decision B).
- FHIR ships **free vendored codes only** (LOINC/ICD/CIEL), no SNOMED, no runtime API.
- `form_id` convention settled: form's `settings.form_id` = `contact:<type>:create`
  (no prefix); `contact_types[].create_form` = `form:contact:<type>:create` (prefix) —
  differ by design. (`docs/reviews/after_hierarchy_and_contacts.md`.)
- Quick hierarchy creator: **linear chain only**; person leaf `parents:[last place]`;
  number = inline seed (not a blocking step). (Plan §13 — confirm against `b4d837b`.)

## Current gates
| Gate | State |
|---|---|
| Typecheck | ✅ green |
| Shared build | ✅ green |
| Shared tests | 333/336 — the 3 fails are FHIR CRLF fixtures (LF clone = 336/336) |
| Lint (zero-warnings) | ❌ ~90 problems (eslint-config browser-globals gap + 17 no-useless-escape) — deferred |

## What's left / next (continue here)
1. **Planner: audit the two newest commits** — `b4d837b` (quick hierarchy creator)
   against `docs/plans/quick-hierarchy-creator.md` (gate-on-empty `contact_types`,
   slug-collision blocking, person `parents:[last]`, non-destructive re-run /
   round-trip, a11y, tests), and spot-check `be8279f` (the wiring fix). **Not yet
   audited.**
2. **Lint + `.gitattributes`** — the only remaining item of the original top-3
   (`*.json eol=lf` fixes the 3 CRLF test fails on clone; then re-add `pnpm lint` to
   CI). DEV-HANDOFF #3.
3. **`client/vite.config.run.mjs`** — throwaway local run config, intentionally
   uncommitted ("not meant to be committed"). Dev to **delete / fold into
   `vite.config.ts` / `.gitignore`** it (DEV-HANDOFF P2). Currently the only thing in
   the working tree.

## Open decisions
- **CIEL dictionary** — OCL paywall (HTTP 403): recommend **defer** (free-codes-only
  stance). OpenMRS public release is a possible later free swap.
- **ICD-10/11** — needs free WHO API creds **and** the WHO-vs-CM ICD-10 variant call
  (clinical owner) before snapshotting.
- **Quick-hierarchy plan §13** — confirm the two recommendations as built in `b4d837b`.

## Deferred by design (don't rebuild)
Lineage Variant B (contact-edit); Phase 1c (conditionReducer ownership); Phase 2b
task `source_id`; targeted-deploy §4 one-click; targets.js / SMS-Devanagari editors
(out of MVP per CLAUDE.md). Full "Done — don't redo" list in DEV-HANDOFF §4.
